{
  "name": "angio",
  "version": "0.1.0",
  "private": true,
  "description": "NGO tree-planting transparency app. Root scripts orchestrate the server and client workspaces.",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names server,client --prefix-colors green,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "9.1.0"
  }
}
